/**
 * Admin features. Same shape as `apps/web/src/features`: one directory per
 * feature, each with `index.ts` (client-safe) and, where it needs one,
 * `server.ts` (server-only) as its entire public surface.
 *
 *   auth/   signing in, and the forced password change
 *   users/  «مدیریت کاربران» — the admin accounts themselves
 */

export {};
