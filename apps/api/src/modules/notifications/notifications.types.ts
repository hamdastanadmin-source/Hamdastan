/**
 * Types internal to the Notifications module.
 *
 * Anything the front-end also has to agree on goes in `@hamdastan/types`
 * instead, so both sides compile against one definition.
 */

export {};
