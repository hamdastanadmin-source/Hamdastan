/**
 * Forms — پاسخ به فرم‌های منتشرشده.
 *
 * The respondent's half of «فرم‌ها و نظرسنجی‌ها». Forms are authored in
 * `apps/admin`; this feature only opens a published one and sends the answers.
 *
 * Client-safe. The server-only lookup is exported from `./server`.
 */

export { PublicFormView } from './components/PublicFormView';
