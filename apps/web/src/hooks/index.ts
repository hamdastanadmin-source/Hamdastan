/**
 * Hooks used across more than one feature of this app.
 *
 * A hook that only one feature needs belongs in that feature's `hooks/`.
 * A hook that `apps/admin` would also want belongs in `@hamdastan/ui`.
 */

export {};
