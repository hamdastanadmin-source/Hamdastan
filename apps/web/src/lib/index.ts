/**
 * App-local infrastructure: wiring and adapters that are specific to this
 * Next.js app and are not business logic.
 *
 * Genuinely reusable helpers belong in `@hamdastan/shared`; anything a single
 * feature uses belongs in that feature's `utils/`.
 */

export {};
