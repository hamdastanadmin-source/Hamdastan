/**
 * Client-side state shared across features (React context, external stores).
 *
 * State owned by one feature belongs in that feature. Server state is not
 * kept here — it comes from `@/services` on each request.
 */

export {};
