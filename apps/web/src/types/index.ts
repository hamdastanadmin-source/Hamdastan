/**
 * Types that belong to this app alone — route params, view models, props
 * shared between its components.
 *
 * Anything the backend also needs to agree on goes in `@hamdastan/types`, so
 * both sides compile against one definition.
 */

export {};
